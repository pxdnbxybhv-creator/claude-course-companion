// STUB — 家园 · the homestead's ground: the plot (map.ts HOME_PLOT), its fence and gate, and
// whatever the owner has built there (app/home.ts). Filled in by the homestead builder.
import type { RegionModule } from '../types';

export const homeRegion: RegionModule = {
  id: 'home',
  build() {},
};
