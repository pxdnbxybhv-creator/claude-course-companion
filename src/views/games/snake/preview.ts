// STUB — a small still painting of the Snake game for its card in the games hall.
// Contract: paint into a canvas already sized to its css box × devicePixelRatio; must be cheap (< 30 ms).
export function paintPreview(canvas: HTMLCanvasElement): void {
  void canvas;
}

/** One short line for the hall card, e.g. best score; null when there's nothing yet. */
export function statLine(lang: 'zh' | 'en'): string | null {
  void lang;
  return null;
}
