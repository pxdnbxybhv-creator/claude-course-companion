// STUB — the Snake game page. Replace with the real game.
import { GameShell } from '../GameShell';

export function SnakeView() {
  return (
    <GameShell titleZh="贪吃蛇" titleEn="Snake" subtitle="…">
      <p class="muted">…</p>
    </GameShell>
  );
}
