// STUB — a small still painting for the Tangram card in the games hall (cheap, < 30 ms).
export function paintPreview(canvas: HTMLCanvasElement): void {
  void canvas;
}
export function statLine(lang: 'zh' | 'en'): string | null {
  void lang;
  return null;
}
